services:
  bot:
    build:
      context: .
      args:
        # Off by default — the dashboard build stages are what make the image
        # need 1 GB instead of 256 MB. Set WITH_DASHBOARD=true in .env (or the
        # environment) on a host that wants the dashboard, and keep
        # ENABLE_DASHBOARD in .env in agreement with it: building without the
        # dashboard and enabling it at runtime yields empty pages.
        WITH_DASHBOARD: ${WITH_DASHBOARD:-false}
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PORT:-3847}:${PORT:-3847}"
    env_file: .env
    environment:
      # Override for Docker-internal networking
      DATABASE_URL: postgres://helyx:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}@postgres:5432/helyx
      OLLAMA_URL: http://host.docker.internal:11434
      OPENCODE_HOST: host.docker.internal
      DOWNLOADS_DIR: /app/downloads
      HOST_DOWNLOADS_DIR: ${HOST_DOWNLOADS_DIR:-${PWD}/downloads}
      KNOWLEDGE_BASE: ${KNOWLEDGE_BASE:-}
      HOST_CLAUDE_CONFIG: /host-claude-config
      HOST_PROJECTS_DIR: ${HOST_PROJECTS_DIR:-${HOME}/bots}
      HOST_HOME: ${HOME}
      PIPER_DIR: /app/piper
      HOME: /home/bun
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./downloads:/app/downloads
      - ${HOME}/.claude:/host-claude-config
      - ${KNOWLEDGE_BASE_PATH:-./.empty}:/app/knowledge-base:ro
      - ./tmux-projects.json:/app/tmux-projects.json
      - ${HOST_PROJECTS_DIR:-${HOME}/bots}:/host-projects
      - ${HOME}:/host-home:ro
      - ./logs:/app/logs
      # Voices only. Mounting the whole ./piper would shadow the runtime that
      # ships inside the image, and the failure is silent — synthesis just
      # stops working on a host whose ./piper is empty.
      - ./piper/voices:/app/piper/voices:ro
    healthcheck:
      test: ["CMD-SHELL", "bun -e \"fetch('http://localhost:${PORT:-3847}/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - helyx

  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: helyx
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: helyx
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U helyx -d helyx"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - helyx

  # Optional: local Whisper ASR fallback (if GROQ_API_KEY is not set)
  # whisper:
  #   image: onerahmet/openai-whisper-asr-webservice:latest
  #   restart: unless-stopped
  #   environment:
  #     ASR_MODEL: medium
  #   healthcheck:
  #     test: ["CMD-SHELL", "curl -sf http://localhost:9000/ || exit 1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 10
  #     start_period: 30s
  #   networks:
  #     - helyx

volumes:
  pgdata:
    name: helyx-pgdata

networks:
  helyx:
